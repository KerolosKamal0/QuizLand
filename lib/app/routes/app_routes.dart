abstract class Routes {
  Routes._();

  static const SPLASH = '/';
  static const LOGIN = '/login';

  // Main shell — owns bottom nav + drawer
  static const MAIN = '/main';

  // Standalone screens pushed on top of shell
  static const HOME = '/home';
}
